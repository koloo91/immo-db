# Nixpacks configuration for Railway / Coolify
[phases.setup]
nixPkgs = ["...", "python3", "gcc", "gnumake", "pkg-config"]
cmds = ["npm install -g node-gyp"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "node .output/server/index.mjs"
